defaults:
  - env: hover        # Task
  - dynamics: a300    # Quadrotor Model
  - renderer: replay
  - _self_



device: "cuda"
dt: 0.01

cm: ctbr          # Control Mode

num_envs: 1
episodes: 20
steps: 800

truncation: 50

